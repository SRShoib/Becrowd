#include <stdio.h>
#include <string.h>
int main(){
    int i, n;
    char q[100];
    char a[] = "gzuz";
    scanf("%d", &n);

    for (i=1; i<=n; i++)
    {
        fgets(q, 100, stdin);
        puts(a);
    }


return 0;
}
